shape parser-missing-brace {
  family "compiler-errors/parser"
  generator "error"
  features ["error", "parser", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  expect_message "missing '}'"
  source ny <<'NY'
use std.core

if true {
   print(1)
NY
}
