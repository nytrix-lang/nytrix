shape args-invalid-heap {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--heap=bogus"
  expect_message "invalid heap policy"
  source ny <<'NY'
use std.core
print("ok")
NY
}
