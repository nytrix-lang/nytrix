shape args-invalid-run-mode {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--run=bogus"
  expect_message "invalid run mode"
  source ny <<'NY'
use std.core
print("ok")
NY
}
