shape args-invalid-runtime-mode {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--mode=bogus"
  expect_message "invalid runtime mode"
  source ny <<'NY'
use std.core
print("ok")
NY
}
