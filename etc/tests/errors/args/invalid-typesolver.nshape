shape args-invalid-typesolver {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--type-solver=bogus"
  expect_message "invalid type solver"
  source ny <<'NY'
use std.core
print("ok")
NY
}
