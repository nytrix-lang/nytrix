shape args-invalid-watch-poll {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--watch-poll=abc"
  expect_message "invalid watch poll interval"
  source ny <<'NY'
use std.core
print("ok")
NY
}
