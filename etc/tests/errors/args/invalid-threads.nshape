shape args-invalid-threads {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--threads=abc"
  expect_message "invalid thread count"
  source ny <<'NY'
use std.core
print("ok")
NY
}
