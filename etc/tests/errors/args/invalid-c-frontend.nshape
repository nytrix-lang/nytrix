shape args-invalid-c-frontend {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--c-frontend=bogus"
  expect_message "invalid C frontend"
  source ny <<'NY'
use std.core
print("ok")
NY
}
