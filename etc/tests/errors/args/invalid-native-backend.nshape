shape args-invalid-native-backend {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--native-backend=bogus"
  expect_message "invalid native backend"
  source ny <<'NY'
use std.core
print("ok")
NY
}
