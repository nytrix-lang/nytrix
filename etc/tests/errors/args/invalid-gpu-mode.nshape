shape args-invalid-gpu-mode {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--gpu=bogus"
  expect_message "unknown gpu mode"
  source ny <<'NY'
use std.core
print("ok")
NY
}
