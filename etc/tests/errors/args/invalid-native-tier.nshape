shape args-invalid-native-tier {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--native-tier=bogus"
  expect_message "invalid native tier"
  source ny <<'NY'
use std.core
print("ok")
NY
}
