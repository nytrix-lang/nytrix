shape args-invalid-color {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--color=bogus"
  expect_message "invalid color mode"
  source ny <<'NY'
use std.core
print("ok")
NY
}
