shape args-invalid-parallel {
  family "compiler-errors/args"
  generator "error"
  features ["args", "cli", "negative", "diagnostic"]
  template ny-compile-fail
  expect compile_fail

  flags "--parallel=bogus"
  expect_message "unknown parallel mode"
  source ny <<'NY'
use std.core
print("ok")
NY
}
