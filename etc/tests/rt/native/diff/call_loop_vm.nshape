shape diff_call_loop_vm {
  family "runtime-native"
  generator "native"
  features [native nyir vm differential call loop]
  template ny-test-case
  flags "--nyir-run"
  expect compile_and_run
  source ny <<'NY'
fn inc(i64 x) i64 { x + 1 }
mut x = 0
while x < 4 { x = inc(x) }
x
NY
}
