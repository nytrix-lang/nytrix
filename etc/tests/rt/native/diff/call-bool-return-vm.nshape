shape diff_call_bool_return_vm {
  family "runtime-native"
  generator "native"
  features [native nyir vm differential call bool]
  template ny-test-case
  flags "--nyir-run"
  expect compile_and_run
  source ny <<'NY'
fn gt(i64 a, i64 b) bool { a > b }
if gt(5, 3) { 7 } else { 9 }
NY
}
