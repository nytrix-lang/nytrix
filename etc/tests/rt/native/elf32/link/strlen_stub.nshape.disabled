shape i386_elf32_object_strlen_stub {
  family "runtime-native"
  generator "native"
  features [native object elf32 x86 nyir pointer strlen]
  template ny-test-case
  flags "--native-backend x86 -emit-only -o /tmp/nytrix-link-run-elf32-strlen-stub.o"
  expect i64_42
  source ny <<'NY'
fn probe() i64 { 42 }

probe()
NY
}
