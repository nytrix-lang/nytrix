shape sanitize_address_aot_exec {
  family "runtime-sanitize"
  generator "runtime"
  features [sanitize address aot execution]
  template ny-test-case
  flags "--sanitize=address --no-progress --color=never"
  expect compile_and_run
  source ny <<'NY'
print(42)
NY
}
